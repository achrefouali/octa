{% extends 'back/layout.html.twig' %}

{% block content %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pay.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_pays_index') }}">back to list</a>

    <a href="{{ path('back_pays_edit', {'id': pay.id}) }}">edit</a>

{% endblock %}
