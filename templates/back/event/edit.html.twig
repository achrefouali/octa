{% extends 'back/layout.html.twig' %}
{% block title %}{{ parent() }}| Modifier un &Eacute;vénnement{% endblock %}
{% block subtitle %} Modifier un &Eacute;vénnement{% endblock %}
{% block content %}

    {% import 'back/event/macros/base_form.html.twig' as  form_macro  %}


    {% set fields ={  0:
        {
            "libelle": "&Eacute;vénnement" ,
            "class":"",
            "value": {
            "logo":"form-control",
            "logoFacture":"form-control",
            "designation":"form-control",
            "designationEn":"form-control",
            "designation2":"form-control",
            "designation2En":"form-control",
            "pays":"form-control",
            "ville":"form-control",
            "adresse":"form-control",
            "longitude":"form-control",
            "latitude":"form-control",
            "description":"form-control ckeditor",
            "descriptionEn":"form-control ckeditor",
            "programFile":"form-control",
            "brochureFile":"form-control",
             "excursion":"form-control",
            "keywords":"form-control",
            "metadescription":"form-control",
            "facebookLink":"form-control",
            "googleLink":"form-control",
            "tweeterLink":"form-control",
            "instagramLink":"form-control",
            "dateDebut":"form-control",
            "dateFin":"form-control",
            "documents":"form-control",
           


        }
        }
    }
    %}
    {{ form_macro.form(form,fields,'form-horizontal form-bordered', path('back_event_edit', {id: event.id}), path('back_event_index'))}}





{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>


        // add-collection-widget.js
        jQuery(document).ready(function () {
            jQuery('.add-another-collection-widget').click(function (e) {
                var list = jQuery(jQuery(this).attr('data-list'));
                // Try to find the counter of the list
                var counter = list.data('widget-counter') | list.children().length;
                // If the counter does not exist, use the length of the list
                if (!counter) { counter = list.children().length; }

                // grab the prototype template
                var newWidget = list.attr('data-prototype');
                // replace the "__name__" used in the id and name of the prototype
                // with a number that's unique to your emails
                // end name attribute looks like name="contact[emails][2]"
                newWidget = newWidget.replace(/__name__/g, counter);
                // Increase the counter
                counter++;
                // And store it, the length cannot be used if deleting widgets is allowed
                list.data(' widget-counter', counter);

                // create a new list element and add it to the list
                var newElem = jQuery(list.attr('data-widget-tags')).html(newWidget);
                newElem.appendTo(list);
            });
        });

    </script>


{% endblock %}